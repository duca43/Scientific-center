import { FormFieldsPipe } from './formfields.pipe';

describe('FormFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
